steps:

# Build Docker image
- name: 'gcr.io/cloud-builders/docker'
  args:
    - 'build'
    - '-t'
    - 'asia-south2-docker.pkg.dev/cicd-tutorial-497406/cicd-repo/myapp:$SHORT_SHA'
    - '.'

# Push Docker image
- name: 'gcr.io/cloud-builders/docker'
  args:
    - 'push'
    - 'asia-south2-docker.pkg.dev/cicd-tutorial-497406/cicd-repo/myapp:$SHORT_SHA'

# Deploy to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
    - 'run'
    - 'deploy'
    - 'myapp'
    - '--image'
    - 'asia-south2-docker.pkg.dev/cicd-tutorial-497406/cicd-repo/myapp:$SHORT_SHA'
    - '--region'
    - 'asia-south2'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated'
    - '--port'
    - '8080'
    - '--service-account'
    - 'cicd-124@cicd-tutorial-497406.iam.gserviceaccount.com'

images:
- 'asia-south2-docker.pkg.dev/cicd-tutorial-497406/cicd-repo/myapp:$SHORT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
